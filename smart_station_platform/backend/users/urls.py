# G:\Web\smart_station_platform\backend\users\urls.py

from django.urls import path
from . import views

urlpatterns = [
    # path('register/', views.register_user, name='register_user'), # 示例：用户注册API，待实现
    # path('profile/', views.user_profile, name='user_profile'),   # 示例：用户资料API，待实现
]